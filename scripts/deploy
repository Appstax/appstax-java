#!/usr/bin/env bash -eu
# Local development deploy

cd "$(dirname "$0")"/..

SOURCE="target/appstax-java-1.0.0-jar-with-dependencies.jar"
TARGET="appstax-java.jar"

JAVASDK="dist/$TARGET"
ANDROID="../appstax-android/src/appstax/libs/$TARGET"

mvn package

cp $SOURCE $JAVASDK
cp $SOURCE $ANDROID
